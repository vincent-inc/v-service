/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterNameReversePipe } from './filter-name-reverse.pipe';

describe('Pipe: FilterNameReversee', () => {
  it('create an instance', () => {
    let pipe = new FilterNameReversePipe();
    expect(pipe).toBeTruthy();
  });
});
